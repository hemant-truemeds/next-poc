import React from "react";
import styles from "./searchSection.module.scss";

const SearchSection = () => {
  return <div className={styles.searchSectionWrapper}>SearchSection</div>;
};

export default SearchSection;
